#include<stdio.h>
#include<math.h>

int main()
{
    int t,n,d[100005],i,j,x,s;
    scanf("%d",&t);
    while(t--)
    {
        s=0;
        scanf("%d",&n);
        for(i=0;i<n;i++)
        {
           scanf("%d",&d[i]);
        }

    }
}
